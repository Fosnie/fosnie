# Drop your built Firecracker assets here

vmlinux        — uncompressed kernel
rootfs.ext4    — minimal Linux+Python image (pandas/numpy/matplotlib/openpyxl) + guest agent

Build per ../../firecracker/README.md. Required only for the code-interpreter tool.
